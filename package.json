{
  "name": "bauzagpt-site",
  "private": true,
  "version": "1.0.0",
  "description": "Sitio BauzaGPT con VitePress (carpeta docs/)",
  "scripts": {
    "docs:dev": "vitepress dev docs --host --port 5173",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --host --port 4173",
    "start": "npm run docs:dev",
    "build": "npm run docs:build",
    "preview": "npm run docs:preview"
  },
  "devDependencies": {
    "vitepress": "^0.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "-": "^0.0.1",
    "firebase": "^12.4.0",
    "g": "^2.0.1",
    "login": "^0.8.0",
    "wrangler": "^4.42.2"
  }
}
